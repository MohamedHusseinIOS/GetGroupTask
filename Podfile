# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GetGroupTask' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GetGroupTask
	pod 'RxSwift'
    pod 'RxCocoa'
    pod 'GoogleMaps'
    pod 'GooglePlaces'
    
  target 'GetGroupTaskTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GetGroupTaskUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
